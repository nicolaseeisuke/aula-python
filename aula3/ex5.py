import math

angulo = float(input('Digite o valor do angulo'))

sen = float(math.sin(angulo))
cos = float(math.cos(angulo))
tng = float(math.tan(angulo))

print(f'O valor de seno é {sen:.2f}, o valor de coseno é {cos:.2f}, o valor da tangente é {tng:.2f} ')
